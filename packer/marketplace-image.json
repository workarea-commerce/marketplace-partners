{
  "variables": {
    "token": "{{env `DIGITALOCEAN_TOKEN`}}",
    "image_name": "workarea-{{timestamp}}",
    "apt_packages": "git ssh tzdata build-essential nodejs libssl-dev libyaml-dev libreadline6-dev zlib1g-dev libncurses5-dev libffi-dev libgdbm-dev autoconf bison xvfb imagemagick jpegoptim ruby2.6 ruby2.6-dev nginx-full nginx ruby-switch elasticsearch mongodb-org openjdk-8-jdk-headless redis",
    "rails_flags": "--skip-spring --skip-active-record --skip-action-cable --skip-puma --skip-coffee --skip-turbolinks --skip-bootsnap --skip-yarn --skip-bundle --force"
  },
  "builders": [
    {
      "type": "digitalocean",
      "api_token": "{{user `token`}}",
      "image": "ubuntu-18-04-x64",
      "region": "nyc3",
      "size": "s-1vcpu-1gb",
      "ssh_username": "root",
      "snapshot_name": "{{user `image_name`}}"
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "files/etc/",
      "destination": "/etc/"
    },
    {
      "type": "file",
      "source": "files/var/",
      "destination": "/var/"
    },
    {
      "type": "shell",
      "inline": [
        "cloud-init status --wait",
        "wget -qO - https://www.mongodb.org/static/pgp/server-4.0.asc | apt-key add -",
        "wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch | apt-key add -",
        "apt -yy install apt-transport-https",
        "apt-add-repository -y ppa:brightbox/ruby-ng",
        "apt -qqy update",
        "apt -qqy -o Dpkg::Options::='--force-confdef' -o Dpkg::Options::='--force-confold' full-upgrade",
        "apt -qqy -o Dpkg::Options::='--force-confdef' -o Dpkg::Options::='--force-confold' install {{user `apt_packages`}}",
        "systemctl daemon-reload",
        "systemctl enable elasticsearch",
        "systemctl enable mongod",
        "systemctl enable rails",
        "systemctl enable sidekiq",
        "systemctl start elasticsearch",
        "systemctl start mongod",
        "ruby-switch --set ruby2.6",
        "gem install bundler --no-doc",
        "mkdir -p /srv/shop",
        "cd /srv/shop",
        "bundle init",
        "echo \"gem 'workarea', github: 'workarea-commerce/workarea', branch: 'v3.4-stable'\" >> Gemfile",
        "bundle install",
        "bundle exec rails new . {{user `rails_flags`}}",
        "echo \"gem 'workarea', github: 'workarea-commerce/workarea', branch: 'v3.4-stable'\" >> Gemfile",
        "bundle remove sass-rails",
        "bundle install",
        "bundle exec rails generate workarea:install",
        "bundle exec rails assets:precompile",
        "echo \"export SECRET_KEY_BASE=$(bundle exec rails secret)\" >> /etc/profile.d/workarea.sh"
      ]
    },
    {
      "type": "shell",
      "scripts": [
        "scripts/01-test",
        "scripts/90-cleanup.sh",
        "scripts/99-img_check.sh"
      ]
    }
  ]
}
